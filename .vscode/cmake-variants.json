{
  "buildType": {
    "default": "debug",
    "choices": { 
      "debug": {
        "short": "Debug",
        "long": "No optimizations",
        "buildType": "customDebug"
      },
      "release": {
        "short": "Release",
        "long": "Full optimizations",
        "buildType": "customRelease"
      }    
    }
  }
}